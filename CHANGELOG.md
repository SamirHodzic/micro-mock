# Changelog

## 0.1.0

- Initial release: local OpenAPI mock server (via Prism), per-status-code chaos testing (independent % per error code) + latency injection, live reload on spec save, a sidebar for controlling it all, a request panel for calling endpoints and inspecting recent traffic, spec-driven request prefill (path/query/body/auth).
